---
date: 2023-07-18 18:55:08
created: 2023-07-18 15:23:06
---

# 스프링 프레임워크

## 개요

### 정의

- 자바(JAVA) 플랫폼을 위한 오픈소스(Open Source) 애플리케이션 프레임워크(Framework)
- 자바 엔터프라이즈 개발을 편하게 해주는 오픈 소스 경량급 애플리케이션 프레임워크
- 자바 개발을 위한 프레임워크로 종속 객체를 생성해주고,  조립해주는 도구
- 자바로 된 프레임워크로 자바SE로 된 자바 객체(POJO)를 자바EE에 의존적이지 않게 연결해주는 역할

<br>

### 주요 기능과 장점

- 경량 컨테이너로서 자바 객체 관리
- POJO 방식의 프레임워크
- 제어 반전 (IoC) 지원
- 의존성 주입 (DI) 지원
- 관점 지향 프로그래밍 (AOP) 지원
- 영속성과 관련된 다양한 서비스 지원
- 확장성이 높음

  

## DI (Dependency Injection, 의존성 주입)

> 객체 간의 의존성을 주입 (스프링 아닌 일반 용어)

<br>

![](Files/image%20251.png)<br>

### Setter Injection

```java
 B b = new B();
 A a = new A();
 a.setB(b);
```

<br>

### Construction Injection

```java
 B b = new B();
 A a = new A(b);
```

<br>

### XML 방식 샘플

```html
<bean id=“record” class=“di.SprRecord”></bean> // bean 객체 생성
<bean id=“view” class=“di.SprRecordView”> // bean 객체 생성
  <property name=“record” ref=“record”></property> // setRecord() 호출 
</bean>
```

<br>

```java
// XML을 파싱하여 컨테이너에 담는 작업
ApplicationContext ctx = new ClassPathXmlApplicationContext(“config.xml”);
RecordView = (RecordView) ctx.getBean(“view”);
```

<br>

Spring 2.0 부터는 annotation을 제공

```java
@Autowired 
ArbitraryClass arbObject;
```

<br>

컴포넌트 스캔을 통해 bean을 객체를 프레임워크에서 관리

```java
@ComponentScan(basePackages = "com.baeldung.componentscan.springapp.animals")
@Configuration
public class SpringComponentScanApp {
   // ...
}
```

<br>

## IoC (Inversion of Control, 제어 역행)

> 외부(컨테이너)에서 제어를 함

<br>

### 빈(Bean)

- 스프링이 제어권을 가지고 직접 만들고 관계를 부여하는 오브젝트.

### 빈 팩토리(Bean Factory)

- 빈(오브젝트)의 생성과 관계 설정 제어를 담당하는 IoC오브젝트
-  좀 더 확장한 애플리케이션 컨텍스트(application context)를 주로 사용

### 애플리케이션 컨텍스트: (IoC 컨테이너 or  스프링 컨테이너)

- DI를 위한 빈 팩토리에 엔터프라이즈 애플리케이션을 개발하는 데 필요한 여러 가지 컨테이너 기능을 추가한 것.

### 설정정보/설정 메타정보

- 구성정보 or 형상정보 (XML)

### 스프링 컨테이너(IoC 컨테이너)

- IoC 방식으로 빈을 관리한다는 의미에서 애플리케이션 컨텍스트나 빈 팩토리를 의미

<br>

## AOP (Aspect Oriented Programming, 관점 지향 프로그래밍)

> 핵심 관심사항(메인)과 공통 관심사항(부가)를 분리  

<br>

![](Files/image%20252.png)<br>

<br>

- 주 업무가 아닌 부가적인 업무가 강한 응집력을 가지고 있는 경우, 소스 관리 및 개발 업무 진행의 복잡해지고, 어려워짐.
- 즉 서비스 추상화가 어려워짐. 이러한 문제를 해결하기 위한 프로그래밍 기법으로 OOP(Object Oriented Programming)의 보완적 개념.
- 횡단 관심사와 이에 영향 받는 객체 간 결합도를 낮추는데 목적이 있다. 쉽게 말해 클래스들이 공통으로 갖는 기능이나 절차 등을 하나의 것으로 묶어 빼내어 별도로 관리하려는 목적.
- 이러한 부가적인 업무의 예로 로그인(Login), 트랜잭션(Transaction), 보안(Security), 캐싱(Caching)과 같은 내부 처리(비지니스, Business) 작업이 있다.

<br>

![](Files/image%20253.png)

<br>

### 참고하면 좋은 사이트

- [\[Spring\] 스프링 AOP (Spring AOP) 총정리 : 개념, 프록시 기반 AOP, @AOP (tistory.com)](https://engkimbs.tistory.com/746)
- [3\. 스프링 AOP (AspectJ의 Pointcut 표현식) : 네이버 블로그 (naver.com)](https://blog.naver.com/PostView.nhn?blogId=chocolleto&logNo=30086024618)
- [스프링 트라이앵글 - AOP (velog.io)](https://velog.io/@woo00oo/%EC%8A%A4%ED%94%84%EB%A7%81-%ED%8A%B8%EB%9D%BC%EC%9D%B4%EC%95%B5%EA%B8%80-AOP)

<br>

## 기타 등등

- ...그냥 저 블로그 보면서 설명하면 나을듯 ㅋㅋㅋ
- [스프링(Spring) 프레임워크 기본 개념 강좌 (1) - 스프링 이해하기 (ooz.co.kr)](https://ooz.co.kr/170)